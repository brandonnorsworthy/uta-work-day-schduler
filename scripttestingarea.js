events: [
    {
        eventDescription: event.target.parentElement.firstChild.textContent,
        eventTime: event.target.parentElement.children[1].textContent,
        eventDate: moment().dayOfYear(),
    }
]

events: {
    dayOfYear: [{
        
    }]
}